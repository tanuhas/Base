@model List<WebApplication5.Models.Domain.Employee>
@{
}

<h1>Сотрудники</h1>

<table class="table">
    <form asp-controller="Employees" asp-action="Index">
        <p>
            Введите должность сотрудника <input type="text" name="SearchString" />
            <input type="submit" value="Поиск" />
        </p>
    </form>
    <thead>
        <tr>
            <th>Идентицикатор сотрудника</th>
            <th>ФИО</th>
            <th>Должность</th>
            <th>Зарплата</th>
            <th>Подробнее</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model)
        {
            <tr>
                <td>@employee.Id</td>
                <td>@employee.FIO</td>
                <td>@employee.Post</td>
                <td>@employee.Salary</td>
                <td><a href="Employees/View/@employee.Id">Подробнее</a></td>
            </tr>
        }
    </tbody>
</table>